# Database Layer

Drizzle ORM, PostgreSQL, Supabase, and Redis are configured in `packages/database`.

- SQL migrations live in `database/migrations`.
- Connection wiring is centralized in `packages/database/src/index.ts`.
