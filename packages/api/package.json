{
  "name": "@lifeos/api",
  "version": "2.0.0",
  "private": true,
  "description": "tRPC API layer for LifeOS — routers, services, and middleware",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./routers": {
      "types": "./src/routers/index.ts",
      "default": "./src/routers/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@lifeos/config": "workspace:*",
    "@lifeos/types": "workspace:*",
    "@lifeos/validators": "workspace:*",
    "@types/node": "^26.1.0",
    "@vitest/coverage-v8": "^2.1.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
